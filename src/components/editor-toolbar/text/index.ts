import { textMain } from "./text";
import { textAlignment } from "./alignment";
import { headings } from "./headings";

export const text = [
    ...textMain,
    ...textAlignment,
    ...headings,
];
